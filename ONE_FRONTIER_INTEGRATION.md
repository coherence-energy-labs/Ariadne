# ONE Frontier integration: directed success contracts

Base: `88ad47819b54079d1965d050fb0d259c2d1b6d9f`. This is an opt-in API, not a replacement for the existing geometric Helmholtz/HJB heuristic.

## Public entry points

```python
import ariadne

rates = [[0, 2, 1], [0, 0, 1], [0, 0, 0]]
answer = ariadne.goal_success(rates, [2], loss_rate=0.2, deadline=3)
model = ariadne.compile_success_contract(
    [[0, 2], [0, 0]], [0.2, 0.3], [0, 1], [0, 1])
answer = model.evaluate([0.1, 0.1], current_fingerprint=model.fingerprint,
                        deadline=3, certify=True)
bundle = model.select_bundle([0, 1], [1, 1], 1, fraction=0.5,
                             current_fingerprint=model.fingerprint)
```

Inputs are **directed continuous-time transition rates**, positive failure rates, and nonnegative absorbing-success rates, all in consistent inverse-time units. Arbitrary k-NN weights are not a calibrated orbital success model. `goal_success` uses actual absorbing goal boundaries, including multiple goals; it does not average or normalize multiple source amplitudes.

`SuccessContract` compiles hidden states by Schur complement. Repeated constant failure-rate reductions use boundary-sized solves. First two resolvent derivatives preserve successful-arrival mean and variance. Explicit deadline queries deliberately use the **full augmented matrix exponential**, because a static Schur complement is not a memoryless time model. Large deadline work is refused rather than running an unbounded exponential computation.

Only failure-rate reductions at declared retained states are supported. Dense preparation is capped at 2,000 states. Budget selection enumerates a finite family of at most 20 sites; integer costs only, explicit candidate cap, and refusal rather than a false optimum on exhaustion. The objective is **eventual** success, not deadline probability. Numerical near-tie ordering is not a proof of unique optimality.

Scalars (`deadline`, `fraction`) are validated by type before any comparison: strings, booleans and complex values are refused with `ValueError`. Probabilities accepted within the solver tolerance (1e-12) are clamped into [0, 1]; each result records the tolerance and the largest adjustment made (`probability_clamp`, `deadline_clamp`). A deadline probability is never reported above the eventual probability.

The caller must supply the current model fingerprint. This is a stale-use tripwire, not proof the caller supplied live telemetry. Hashing uses a schema-bound, canonical binary64 little-endian representation including shape and normalizing signed zero; entries that binary64 cannot hold exactly (for example `Fraction(1, 1000)`) are additionally bound by their exact numerator and denominator, so a rational model and its rounded float copy have different fingerprints.

## What the certificate proves

Solves run in binary64. The final-answer checker does not: it rebuilds the **entire intervened generator in exact rational arithmetic** from the exact value of every supplied rate and reduction, recomputes every row sum (and, in `goal_success`, every goal-flux sum) exactly, evaluates the residual of the floating candidate exactly, and bounds the probability error by strict row diagonal dominance. It uses nothing from the producer's factorization or from the binary64 generator's rounding.

What "exact value" means is precise. `Fraction`, `int` and `Decimal` rates are taken exactly as given, so the bound certifies the caller's rational model. A binary64 rate is taken at its exact binary value: a literal `0.1` is certified as the binary64 number it became, not as one tenth. The certificate reports `exact_rational_entries`, the number of supplied entries binary64 could not hold.

This matters in practice, not only in principle. With rates `W=[[0,7],[11,0]]`, failure `[1e-3, 1e-6]`, success `[1e-6, 1e-3]`, binary64 rounds the diagonal `7 + 0.001 + 1e-06`, and the earlier certificate (computed on that rounded matrix) returned an interval that **excluded** the exact model's probability. That witness is now a regression test (`test_certificate_contains_the_exact_model_when_float_row_sums_round`).

It is a computational certificate for that exact rational model, not proof of the physical rate model or of the timing moments. It costs rational arithmetic over the generator's nonzeros and is deliberately charged separately from repeated-query timing.

## Measured source-level evidence

Every number below is produced by `python scripts/one_frontier_replay.py` (seeded) and rendered from its output; `python scripts/one_frontier_replay.py --check` re-runs the deterministic sections and compares them with the recorded values.

<!-- BEGIN GENERATED by scripts/one_frontier_replay.py; edit the script, not this block -->
Recorded with Python 3.13.14, NumPy 2.5.3, SciPy 1.18.1 on `Windows-11-10.0.26200-SP0`, one BLAS thread. Raw values: `docs/one_frontier_replay.json`.

**Focused suite** (`tests/test_success_contract.py`): 115 passed, 0 failed, 0 skipped.

**Independent oracles** (seeds 10000..10199): 1600 directed-instance intervention queries against full dense solves, maximum absolute probability discrepancy 4.194e-15; 200 of them also against a dense matrix exponential, maximum deadline discrepancy 7.633e-16. Declared ceiling 1e-12.

**Event-driven simulation** (W=[[0,3],[1,0]], failure=[0.2,0.1], success=[0,2], start 0, deadline 2; seed 20260907, 100,000 trajectories). Eventual success predicted 0.8670520231 (closed form 0.8670520231), observed 0.86675, z = -0.28. Deadline success predicted 0.7953009066, observed 0.79698, z = +1.32. A synthetic CTMC, not an orbital hardware result.

**Source mutants** (each applied to a disposable copy; killed = failing assertions, collection errors never count):

| mutant | verdict | failing focused tests |
|---|---|---|
| `stale_model_bypass` | killed | 22 |
| `deadline_clock_sign` | killed | 55 |
| `eventual_reported_as_deadline` | killed | 53 |
| `certificate_on_float_row_sums` | killed | 2 |
| `probability_not_clamped` | killed | 3 |
| `deadline_type_unchecked` | killed | 3 |

**Timing** (MEASURED, machine-dependent, never checked by `--check`): 300 states, 11 retained, 56 queries, median of 5 repeats. Preparation 5.883 ms, queries 3.582 ms (eventual probability + successful-time moments; no certificate, no deadline); fresh sparse full re-solves 162.999 ms; a conventional prepared Woodbury comparator 2.351 ms + 0.909 ms for eventual probability only. Maximum disagreement 5.00e-16. No universal speed or new-algorithm claim.
<!-- END GENERATED -->

Exact base root/constants source blobs were verified; original root API text is preserved before two lazy additions. The existing coherence_hjb.py is unchanged.

## Review boundary

The new Ubuntu/Windows focused workflow exercises this module and the root facade. Full repository suites were run separately; their results are reported with the change that records them, not here. GPU, CR3BP operational mission calibration and physical experiments are NOT claimed. Independent full CI and maintainer review are required before merge/deployment. This adds no new force model, proof tier, production switch or live ACE action.

Reproduce the focused suite: `python -m pytest -q -o addopts= tests/test_success_contract.py`.
